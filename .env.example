# Postgres connection string. On Vercel, set this in Project Settings ->
# Environment Variables (use the pooled connection string for serverless).
# Locally, point it at any Postgres instance.
DATABASE_URL="postgresql://user:password@localhost:5432/estimator?schema=public"
